<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:luna="https://github.com/curryzxh/Luna">
  <Style Selector="luna|Tag">
    <Setter Property="Focusable" Value="False" />
    <Setter Property="Template">
      <ControlTemplate>
        <Border Name="PART_Root"
                IsVisible="{TemplateBinding IsOpen}">
          <StackPanel Orientation="Horizontal"
                      Spacing="6"
                      VerticalAlignment="Center">
            <ContentPresenter Content="{TemplateBinding Icon}"
                              IsVisible="{TemplateBinding IsIconVisible}"
                              VerticalAlignment="Center" />
            <ContentPresenter Content="{TemplateBinding Content}"
                              ContentTemplate="{TemplateBinding ContentTemplate}"
                              VerticalAlignment="Center" />
            <Button Name="PART_CloseButton"
                    Classes="text"
                    Content="×"
                    IsVisible="{TemplateBinding IsClosable}"
                    VerticalAlignment="Center" />
          </StackPanel>
        </Border>
      </ControlTemplate>
    </Setter>
  </Style>

  <Style Selector="luna|Tag /template/ Border#PART_Root">
    <Setter Property="Background" Value="{DynamicResource Luna.Brush.Background.Container}" />
    <Setter Property="BorderBrush" Value="{DynamicResource Luna.Brush.Border.Default}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="CornerRadius" Value="{DynamicResource Luna.Radius.Default}" />
    <Setter Property="Padding" Value="8,3" />
    <Setter Property="TextElement.FontSize" Value="{DynamicResource Luna.FontSize.Body.Small}" />
    <Setter Property="TextElement.FontWeight" Value="{DynamicResource Luna.FontWeight.Medium}" />
    <Setter Property="TextElement.Foreground" Value="{DynamicResource Luna.Brush.Text.Primary}" />
  </Style>

  <Style Selector="luna|Tag:small /template/ Border#PART_Root">
    <Setter Property="Padding" Value="6,2" />
    <Setter Property="TextElement.FontSize" Value="{DynamicResource Luna.FontSize.Mark.Small}" />
  </Style>

  <Style Selector="luna|Tag:medium /template/ Border#PART_Root">
    <Setter Property="Padding" Value="8,3" />
    <Setter Property="TextElement.FontSize" Value="{DynamicResource Luna.FontSize.Body.Small}" />
  </Style>

  <Style Selector="luna|Tag:large /template/ Border#PART_Root">
    <Setter Property="Padding" Value="10,4" />
    <Setter Property="TextElement.FontSize" Value="{DynamicResource Luna.FontSize.Body.Small}" />
  </Style>

  <Style Selector="luna|Tag:extra-large /template/ Border#PART_Root">
    <Setter Property="Padding" Value="12,6" />
    <Setter Property="TextElement.FontSize" Value="{DynamicResource Luna.FontSize.Body.Medium}" />
  </Style>

  <Style Selector="luna|Tag:round /template/ Border#PART_Root">
    <Setter Property="CornerRadius" Value="{DynamicResource Luna.Radius.Circle}" />
  </Style>

  <Style Selector="luna|Tag:mark /template/ Border#PART_Root">
    <Setter Property="CornerRadius" Value="6,6,6,0" />
  </Style>

  <Style Selector="luna|Tag:outline /template/ Border#PART_Root">
    <Setter Property="Background" Value="Transparent" />
  </Style>

  <Style Selector="luna|Tag:dark /template/ Border#PART_Root">
    <Setter Property="TextElement.Foreground" Value="{DynamicResource Luna.Brush.Text.Anti}" />
  </Style>

  <Style Selector="luna|Tag:default /template/ Border#PART_Root">
    <Setter Property="BorderBrush" Value="{DynamicResource Luna.Brush.Border.Strong}" />
  </Style>

  <Style Selector="luna|Tag:primary /template/ Border#PART_Root">
    <Setter Property="BorderBrush" Value="{DynamicResource Luna.Brush.Brand}" />
    <Setter Property="TextElement.Foreground" Value="{DynamicResource Luna.Brush.Brand}" />
  </Style>

  <Style Selector="luna|Tag:warning /template/ Border#PART_Root">
    <Setter Property="BorderBrush" Value="{DynamicResource Luna.Brush.Warning}" />
    <Setter Property="TextElement.Foreground" Value="{DynamicResource Luna.Brush.Warning}" />
  </Style>

  <Style Selector="luna|Tag:danger /template/ Border#PART_Root">
    <Setter Property="BorderBrush" Value="{DynamicResource Luna.Brush.Error}" />
    <Setter Property="TextElement.Foreground" Value="{DynamicResource Luna.Brush.Error}" />
  </Style>

  <Style Selector="luna|Tag:success /template/ Border#PART_Root">
    <Setter Property="BorderBrush" Value="{DynamicResource Luna.Brush.Success}" />
    <Setter Property="TextElement.Foreground" Value="{DynamicResource Luna.Brush.Success}" />
  </Style>

  <Style Selector="luna|Tag:primary:dark /template/ Border#PART_Root">
    <Setter Property="Background" Value="{DynamicResource Luna.Brush.Brand}" />
    <Setter Property="BorderBrush" Value="{DynamicResource Luna.Brush.Brand}" />
    <Setter Property="TextElement.Foreground" Value="{DynamicResource Luna.Brush.Text.Anti}" />
  </Style>

  <Style Selector="luna|Tag:warning:dark /template/ Border#PART_Root">
    <Setter Property="Background" Value="{DynamicResource Luna.Brush.Warning}" />
    <Setter Property="BorderBrush" Value="{DynamicResource Luna.Brush.Warning}" />
    <Setter Property="TextElement.Foreground" Value="{DynamicResource Luna.Brush.Text.Anti}" />
  </Style>

  <Style Selector="luna|Tag:danger:dark /template/ Border#PART_Root">
    <Setter Property="Background" Value="{DynamicResource Luna.Brush.Error}" />
    <Setter Property="BorderBrush" Value="{DynamicResource Luna.Brush.Error}" />
    <Setter Property="TextElement.Foreground" Value="{DynamicResource Luna.Brush.Text.Anti}" />
  </Style>

  <Style Selector="luna|Tag:success:dark /template/ Border#PART_Root">
    <Setter Property="Background" Value="{DynamicResource Luna.Brush.Success}" />
    <Setter Property="BorderBrush" Value="{DynamicResource Luna.Brush.Success}" />
    <Setter Property="TextElement.Foreground" Value="{DynamicResource Luna.Brush.Text.Anti}" />
  </Style>

  <Style Selector="luna|Tag:light-outline /template/ Border#PART_Root">
    <Setter Property="Background" Value="{DynamicResource Luna.Brush.Background.Container}" />
  </Style>

  <Style Selector="luna|Tag:primary:light-outline /template/ Border#PART_Root">
    <Setter Property="Background" Value="{DynamicResource Luna.Brush.Brand.Light}" />
  </Style>

  <Style Selector="luna|Tag /template/ Button#PART_CloseButton">
    <Setter Property="Padding" Value="0" />
    <Setter Property="MinHeight" Value="0" />
    <Setter Property="MinWidth" Value="0" />
    <Setter Property="Margin" Value="4,0,0,0" />
    <Setter Property="FontSize" Value="14" />
    <Setter Property="Foreground" Value="{DynamicResource Luna.Brush.Text.Primary}" />
  </Style>

  <Style Selector="luna|Tag:primary /template/ Button#PART_CloseButton">
    <Setter Property="Foreground" Value="{DynamicResource Luna.Brush.Brand}" />
  </Style>

  <Style Selector="luna|Tag:warning /template/ Button#PART_CloseButton">
    <Setter Property="Foreground" Value="{DynamicResource Luna.Brush.Warning}" />
  </Style>

  <Style Selector="luna|Tag:danger /template/ Button#PART_CloseButton">
    <Setter Property="Foreground" Value="{DynamicResource Luna.Brush.Error}" />
  </Style>

  <Style Selector="luna|Tag:success /template/ Button#PART_CloseButton">
    <Setter Property="Foreground" Value="{DynamicResource Luna.Brush.Success}" />
  </Style>

  <Style Selector="luna|Tag:dark /template/ Button#PART_CloseButton">
    <Setter Property="Foreground" Value="{DynamicResource Luna.Brush.Text.Anti}" />
  </Style>

  <Style Selector="luna|CheckTag">
    <Setter Property="Focusable" Value="False" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderBrush" Value="Transparent" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="Template">
      <ControlTemplate>
        <Border Name="PART_Root">
          <StackPanel Orientation="Horizontal"
                      Spacing="6"
                      VerticalAlignment="Center">
            <ContentPresenter Content="{TemplateBinding Icon}"
                              IsVisible="{TemplateBinding IsIconVisible}"
                              VerticalAlignment="Center" />
            <ContentPresenter Content="{TemplateBinding Content}"
                              ContentTemplate="{TemplateBinding ContentTemplate}"
                              VerticalAlignment="Center" />
          </StackPanel>
        </Border>
      </ControlTemplate>
    </Setter>
  </Style>

  <Style Selector="luna|CheckTag /template/ Border#PART_Root">
    <Setter Property="Background" Value="{DynamicResource Luna.Brush.Background.Container}" />
    <Setter Property="BorderBrush" Value="{DynamicResource Luna.Brush.Border.Default}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="CornerRadius" Value="{DynamicResource Luna.Radius.Default}" />
    <Setter Property="Padding" Value="10,4" />
    <Setter Property="TextElement.FontSize" Value="{DynamicResource Luna.FontSize.Body.Small}" />
    <Setter Property="TextElement.FontWeight" Value="{DynamicResource Luna.FontWeight.Medium}" />
    <Setter Property="TextElement.Foreground" Value="{DynamicResource Luna.Brush.Text.Primary}" />
  </Style>

  <Style Selector="luna|CheckTag:round /template/ Border#PART_Root">
    <Setter Property="CornerRadius" Value="{DynamicResource Luna.Radius.Circle}" />
  </Style>

  <Style Selector="luna|CheckTag:mark /template/ Border#PART_Root">
    <Setter Property="CornerRadius" Value="6,6,6,0" />
  </Style>

  <Style Selector="luna|CheckTag:outline /template/ Border#PART_Root">
    <Setter Property="Background" Value="Transparent" />
  </Style>

  <Style Selector="luna|CheckTag:default /template/ Border#PART_Root">
    <Setter Property="BorderBrush" Value="{DynamicResource Luna.Brush.Border.Strong}" />
  </Style>

  <Style Selector="luna|CheckTag:primary /template/ Border#PART_Root">
    <Setter Property="BorderBrush" Value="{DynamicResource Luna.Brush.Brand}" />
    <Setter Property="TextElement.Foreground" Value="{DynamicResource Luna.Brush.Brand}" />
  </Style>

  <Style Selector="luna|CheckTag:warning /template/ Border#PART_Root">
    <Setter Property="BorderBrush" Value="{DynamicResource Luna.Brush.Warning}" />
    <Setter Property="TextElement.Foreground" Value="{DynamicResource Luna.Brush.Warning}" />
  </Style>

  <Style Selector="luna|CheckTag:danger /template/ Border#PART_Root">
    <Setter Property="BorderBrush" Value="{DynamicResource Luna.Brush.Error}" />
    <Setter Property="TextElement.Foreground" Value="{DynamicResource Luna.Brush.Error}" />
  </Style>

  <Style Selector="luna|CheckTag:success /template/ Border#PART_Root">
    <Setter Property="BorderBrush" Value="{DynamicResource Luna.Brush.Success}" />
    <Setter Property="TextElement.Foreground" Value="{DynamicResource Luna.Brush.Success}" />
  </Style>

  <Style Selector="luna|CheckTag:checked /template/ Border#PART_Root">
    <Setter Property="TextElement.Foreground" Value="{DynamicResource Luna.Brush.Text.Anti}" />
  </Style>

  <Style Selector="luna|CheckTag:primary:checked /template/ Border#PART_Root">
    <Setter Property="Background" Value="{DynamicResource Luna.Brush.Brand}" />
    <Setter Property="BorderBrush" Value="{DynamicResource Luna.Brush.Brand}" />
  </Style>

  <Style Selector="luna|CheckTag:warning:checked /template/ Border#PART_Root">
    <Setter Property="Background" Value="{DynamicResource Luna.Brush.Warning}" />
    <Setter Property="BorderBrush" Value="{DynamicResource Luna.Brush.Warning}" />
  </Style>

  <Style Selector="luna|CheckTag:danger:checked /template/ Border#PART_Root">
    <Setter Property="Background" Value="{DynamicResource Luna.Brush.Error}" />
    <Setter Property="BorderBrush" Value="{DynamicResource Luna.Brush.Error}" />
  </Style>

  <Style Selector="luna|CheckTag:success:checked /template/ Border#PART_Root">
    <Setter Property="Background" Value="{DynamicResource Luna.Brush.Success}" />
    <Setter Property="BorderBrush" Value="{DynamicResource Luna.Brush.Success}" />
  </Style>
</Styles>
